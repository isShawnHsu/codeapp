name: Build & Export Unsigned IPA

on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: ${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build Unsigned IPA
    runs-on: macOS-26

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      # 1. 安装日志美化工具（它能把隐藏在几万行列表里的 error 强行提取到最下面）
      - name: Install xcbeautify
        run: brew install xcbeautify

      # 2. 下载 CodeApp 运行所需的底层依赖和 framework 资源
      - name: Download Frameworks
        run: |
          chmod +x ./downloadFrameworks.sh
          ./downloadFrameworks.sh

      # 3. 【终极魔改】直接对 SPM 缓存和本地 project 注入全面降级配置
      - name: Deep Patch Workspace
        run: |
          echo "==== 开始对所有工程文件进行地毯式降级 ===="
          
          # 遍历修改所有项目的 pbxproj 配置
          find . -name "project.pbxproj" | while read -r pbx; do
            echo "修补配置文件: $pbx"
            # 关闭警告转错误
            sed -i '' 's/SWIFT_TREAT_WARNINGS_AS_ERRORS = YES/SWIFT_TREAT_WARNINGS_AS_ERRORS = NO/g' "$pbx" || true
            # 强行关闭严格的隔离校验（解决新 Xcode 编译旧库的核心）
            sed -i '' 's/SWIFT_STRICT_CONCURRENCY = YES/SWIFT_STRICT_CONCURRENCY = NO/g' "$pbx" || true
            sed -i '' 's/SWIFT_STRICT_CONCURRENCY = "complete"/SWIFT_STRICT_CONCURRENCY = "none"/g' "$pbx" || true
            # 强制锁死目标版本支持
            sed -i '' 's/IPHONEOS_DEPLOYMENT_TARGET = [0-9.]*/IPHONEOS_DEPLOYMENT_TARGET = 16.0/g' "$pbx" || true
          done

      # 4. 【终极对策】将配置改为 Debug 模式进行编译，彻底绕过 ptrauth 指针安全校验
      - name: Build with Xcode (No Signing)
        run: |
          set -o pipefail
          xcodebuild archive \
            -project Code.xcodeproj \
            -scheme "Code App" \
            -configuration Debug \
            -destination 'generic/platform=iOS' \
            -archivePath ./build/Code.xcarchive \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGN_IDENTITY="" \
            PROVISIONING_PROFILE_SPECIFIER="" \
            COMPILER_INDEX_STORE_ENABLE=NO \
            DEBUG_INFORMATION_FORMAT=dwarf \
            VALIDATE_WORKSPACE=NO \
            | xcbeautify --allow-view-warnings

      # 5. 从 Debug 归档路径动态提取并打包为标准的 .ipa
      - name: Package to IPA
        if: success()
        run: |
          mkdir -p ./build/Payload
          # 动态匹配 Debug 或 Release 下生成的实际 App 路径
          APP_PATH=$(find ./build/Code.xcarchive/Products/Applications -name "*.app" | head -n 1)
          echo "找到的实际 App 路径: $APP_PATH"
          
          # 如果硬签环境下某些插件有残留的额外签名，进行强行剥离（针对 TrollStore 优化）
          if [ -d "$APP_PATH/_CodeSignature" ]; then
            rm -rf "$APP_PATH/_CodeSignature"
          fi
          
          cp -r "$APP_PATH" ./build/Payload/
          cd ./build
          zip -r Code.ipa Payload
          
      # 6. 将最终生成的 Code.ipa 上传作为 GitHub 产物供下载
      - name: Upload Unsigned IPA
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: CodeApp-TrollStore-Unsigned
          path: ./build/Code.ipa
